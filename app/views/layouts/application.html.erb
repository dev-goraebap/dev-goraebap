<!DOCTYPE html>
<html data-theme="lemonade" class="h-full">
  <head>
    <title><%= content_for(:title) || "dev.goraebap" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= favicon_link_tag 'favicon.ico' %>
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-full text-slate-700">    

    <main class="flex-grow overflow-hidden">
      <%= yield %>
    </main>
    
    <!-- 푸터 -->
    <footer class="bg-red-50 py-8 sm:py-12 px-4 mt-auto">
      <div class="w-full max-w-5xl mx-auto flex flex-col md:flex-row md:justify-between md:items-center gap-6 md:gap-0">
        <!-- 로고 및 소개 -->
        <div class="flex flex-col items-center md:items-start">
          <div class="font-bold text-xl text-gray-800 mb-2 md:mb-3">dev.goraebap</div>
          <p class="text-gray-600 text-xs sm:text-sm text-center md:text-left max-w-[300px] md:max-w-none">
            <span class="text-gray-500">🚀 코딩 중... • 🔊 현재 상태: 버그와 싸우는 중 • ✨ 마지막 커밋: 방금 전</span>
          </p>
        </div>
        
        <!-- 연락처 및 소셜 -->
        <div class="flex items-center justify-center md:justify-start">
          <a href="https://github.com/dev-goraebap" target="_blank" class="transition-transform hover:scale-110">
            <img src="<%= asset_url 'github.gif' %>" class="h-8 sm:h-10" alt="GitHub" />
          </a>
        </div>
      </div>
      
      <!-- 저작권 정보 -->
      <div class="mt-8 md:mt-12 pt-6 border-t border-gray-200 text-center">
        <p class="text-gray-500 text-xs sm:text-sm">
          &copy; <%= Date.today.year %> dev.goraebap. All rights reserved.
        </p>
        <p class="text-gray-400 text-xs mt-2">
          Created By <span class="text-base-content">dev.goraebap</span>
        </p>
      </div>
    </footer>
  </body>
</html>