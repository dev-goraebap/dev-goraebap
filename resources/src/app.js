// ---------------------------------------------
// Vite Entry Point
// ---------------------------------------------

import * as Turbo from '@hotwired/turbo';
import Alpine from 'alpinejs';

window.Alpine = Alpine;
Alpine.start();

window.Turbo = Turbo;
