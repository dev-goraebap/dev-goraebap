<ul class="flex flex-col gap-4">
  @each((post, index) in posts)
    @if(index !== 0)
      <li class="border-t border-base-300 sm:hidden">
      </li>
    @end
    <li>
      @!partials.posts.card({ post, collectionUrl: `/series/${ post?.seriesPosts[0]?.series?.slug }` })
    </li>
  @end
</ul>
