<!DOCTYPE html>
<html lang="ko" data-theme="cupcake">

<head>
  @!partials.head({ title, description })
  @if ($slots.meta)
    {{{ await $slots.meta() }}}
  @endif
</head>

<body>
  {{{ await $slots.main() }}}
</body>

</html>