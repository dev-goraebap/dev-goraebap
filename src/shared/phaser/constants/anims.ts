export enum BirdAnims {
    Idle = 'birdIdle',
    Move = 'birdMove',
}

export enum BookTableAnims {
    Idle = 'bookTableIdle',
    Select = 'bookTableSelect',
}

export enum ComputerAnims {
    Idle = 'computerIdle',
    Select = 'computerSelect',
}

export enum FireplaceAnims {
    Idle = 'fireplaceIdle'
}

export enum GameMachineAnims {
    Idle = 'gameMachineIdle',
    Select = 'gameMachineSelect',
}

export enum PianoAnims {
    Idle = 'pianoIdle',
    Select = 'pianoSelect',
}

